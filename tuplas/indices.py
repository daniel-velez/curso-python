#Las tuplas son inmutables, después de ser creadas no se pueden modificar.

tupla = (1, 2, 3, 4, 5, 6, 7, 8, 9, 0);

elemento = tupla[-1];#Retorna el último elemento.
#print(elemento);

#Las tuplas funcionan de manera similar a las listas.

sub = tupla[:9:2];#SUb tupla con los primeros 9 elementos y saltos dobles.
#print(sub);

